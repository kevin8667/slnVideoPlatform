@model Backstage.Models.GenreList

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>GenreList</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.GenreName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.GenreName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.GenreId">Edit</a> |
    <a asp-action="Index" class="btn btn-primary">回到列表</a>
</div>
